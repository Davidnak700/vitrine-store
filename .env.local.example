# Copy this file to .env.local and paste your token after the "=".
#
#   .env.local is gitignored. This file is the committed template and must
#   never hold a real value.
#
# Used by: npm run images:generate (scripts/generate-images.ts)
# Token from: https://huggingface.co/settings/tokens — a read token is enough
# for the Inference API.

HF_TOKEN=
